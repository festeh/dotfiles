# ccs — Claude Code session browser (~/projects/ccs)
# Flags passed to `claude` when resuming a session with Enter/r.

[resume]
# Reasoning effort: low, medium, high, xhigh, max (claude --effort)
effort = "xhigh"
# Skip all permission prompts (claude --dangerously-skip-permissions)
yolo = true
